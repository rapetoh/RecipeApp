export default function Home() {
  return (
    <div style={{ padding: '2rem', fontFamily: 'system-ui' }}>
      <h1>Recipe App API Server</h1>
      <p>This is the backend API server for the Recipe App.</p>
      <p>API endpoints are available at <code>/api/*</code></p>
    </div>
  );
}

